# Content format: [name]

One or two lines: what the format is and the feeling it gives. Name the format
reference in the wild if there is one (we borrow shapes, not words).

## Why it's ours and not a copy

What makes our version different from every other page running the same format.
Usually: the shape is borrowed, the content is specific to running, lifting,
and a real-life schedule.

## The rule (the test)

The one filter that keeps this format on brand. For most formats it's some
version of: if a random big fitness page could post the exact same thing,
rewrite it until they couldn't.

## Bank

Vetted examples, ready to use. Group them if there are a lot (by theme, by
content pillar, by tight-default vs extended). Flag any that are relatable but
generic ("use sparingly") and any that look right but break the format ("does
not fit").

## Visual

Color, placement, photo vs graphic, the treatment. Keep it in the brand system
(Sunrise orange accent, dark and urban, real over posed).

## Caption

The caption energy for this format, with one or two ready examples. Light,
self-aware, easy to read. Add a soft CTA line when the goal is comments/saves.

## Tone guardrails

The cheese traps specific to this format and how to dodge them. Full rules in
`anti-ai-writing`.
